import { Contract, Event } from './index'

export function appendToQueue(contract: Contract, event: Event, payload: any) {
  // const queueRef = getDatabase().ref(`queue/indexer`)
  // queueRef.push({
  //   contract,
  //   event,
  //   payload,
  // })
}
